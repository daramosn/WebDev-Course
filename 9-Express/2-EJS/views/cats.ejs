<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cats</title>
</head>

<body>
    <h1>All the cats: </h1>
    <ul>
        <% cats.forEach(cat => { %>
        <li> Cat <%= cat %> </li>
        <% }) %>
    </ul>

    <ol>
        <% for (const kitty of cats) { %>
        <li> The name of the kitty is <%= kitty %> </li>
        <% } %>
    </ol>
</body>

</html>