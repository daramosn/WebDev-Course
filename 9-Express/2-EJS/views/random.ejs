<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random</title>
</head>

<body>
    <h1>Your random number is <%= Math.floor(Math.random()*10)+1 %>
    </h1>

    <h2>Other method, sending an object from js: <%= num %>
    </h2>

    <% if (num % 2===0) { %>
    <h2>This is a even number! :B</h2>
    <% } else { %>
    <h3>Odd number D:</h3>
    <% } %>


    <hr>
    <h2><%= num%2===0 ? `It's even` : `It's odd` %></h2>

</body>

</html>